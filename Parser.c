#include <stdio.h>
#include <stdlib.h>

int get_next_command(int **arr, int **fixed, int **solution, int dimension, int rowsPerBlock, int colsPerBlock){
    int n = 1024;
    char* str[n];
    fgets(str,n,stdin);
    printf("%s", str);
}